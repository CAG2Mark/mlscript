:js

open annotations { compile }

class Box()
//│ /!\ Warning: object identity between equal objects was not preserved by HandlerLowering
//│ /!\ Offending: ClsLikeDefn:
//│   isym = class:Box⁰
//│   sym = member:Box¹
//│   ctorSym = S of term:Box²
//│   k = Cls
//│   paramsOpt = S of ParamList: ‹empty›

// Now the `@compile` annotation applies to all patterns, while before it could
// only be used for pattern symbols. Now it expands and compiles the following
// pattern.

fun foo(x) = if x is @compile Box then "yes" else "no"

object Singleton
//│ /!\ Warning: object identity between equal objects was not preserved by HandlerLowering
//│ /!\ Offending: ClsLikeDefn:
//│   isym = object:Singleton⁰
//│   sym = member:Singleton¹
//│   k = Obj

fun foo(x) = if x is @compile Singleton then "yes" else "no"


fun foo(x) = if x is @compile 42 then "yes" else "no"

object do_not_compile
//│ /!\ Warning: object identity between equal objects was not preserved by HandlerLowering
//│ /!\ Offending: ClsLikeDefn:
//│   isym = object:do_not_compile⁰
//│   sym = member:do_not_compile¹
//│   k = Obj

:w
fun foo(x) = if x is @do_not_compile Box then "yes" else "no"
//│ ╔══[WARNING] This annotation is not supported here.
//│ ║  l.40: 	fun foo(x) = if x is @do_not_compile Box then "yes" else "no"
//│ ║        	                     ^^^^^^^^^^^^^^^
//│ ╟── Note: Patterns only support the `@compile` annotation.
//│ ║  l.40: 	fun foo(x) = if x is @do_not_compile Box then "yes" else "no"
//│ ╙──      	                                     ^^^

:w
:e
fun foo(x) = if x is @name_not_found Box then "yes" else "no"
//│ ╔══[COMPILATION ERROR] Name not found: name_not_found
//│ ║  l.50: 	fun foo(x) = if x is @name_not_found Box then "yes" else "no"
//│ ╙──      	                     ^^^^^^^^^^^^^^^
//│ ╔══[WARNING] This annotation is not supported here.
//│ ║  l.50: 	fun foo(x) = if x is @name_not_found Box then "yes" else "no"
//│ ║        	                     ^^^^^^^^^^^^^^^^^^^
//│ ╟── Note: Patterns only support the `@compile` annotation.
//│ ║  l.50: 	fun foo(x) = if x is @name_not_found Box then "yes" else "no"
//│ ╙──      	                                     ^^^

pattern Box' = Box
//│ /!\ Warning: object identity between equal objects was not preserved by HandlerLowering
//│ /!\ Offending: ClsLikeDefn:
//│   isym = pattern:Box'⁰
//│   sym = member:Box'¹
//│   k = Pat
//│   methods = Ls of
//│     FunDefn:
//│       sym = member:unapply⁰
//│       dSym = term:unapply¹
//│       params = Ls of
//│         ParamList:
//│           params = Ls of
//│             Param:
//│               sym = input⁰
//│               modulefulness = Modulefulness of N
//│       body = Match:
//│         scrut = SimpleRef of input⁰
//│         arms = Ls of
//│           Tuple2:
//│             _1 = Cls:
//│               cls = class:Box⁰
//│               path = MemberRef{disamb=class:Box⁰}:
//│                 bms = member:Box¹
//│                 disamb = class:Box⁰
//│             _2 = Return of Instantiate:
//│               mut = false
//│               cls = Select{sym=class:MatchSuccess⁰}:
//│                 qual = SimpleRef of tmp:runtime⁰
//│                 name = Ident of "MatchSuccess"
//│               argss = Ls of
//│                 Ls of
//│                   Arg:
//│                     value = SimpleRef of input⁰
//│                   Arg:
//│                     value = Lit of UnitLit of true
//│         dflt = S of Return of Instantiate:
//│           mut = false
//│           cls = Select{sym=class:MatchFailure⁰}:
//│             qual = SimpleRef of tmp:runtime⁰
//│             name = Ident of "MatchFailure"
//│           argss = Ls of
//│             Ls of
//│               Arg:
//│                 value = Lit of UnitLit of true
//│     FunDefn:
//│       sym = member:unapplyStringPrefix⁰
//│       dSym = term:unapplyStringPrefix¹
//│       params = Ls of
//│         ParamList:
//│           params = Ls of
//│             Param:
//│               sym = input¹
//│               modulefulness = Modulefulness of N
//│       body = Return of Instantiate:
//│         mut = false
//│         cls = Select{sym=class:MatchFailure⁰}:
//│           qual = SimpleRef of tmp:runtime⁰
//│           name = Ident of "MatchFailure"
//│         argss = Ls of
//│           Ls of
//│             Arg:
//│               value = Lit of UnitLit of true

fun foo(x) = if x is @compile Box' then "yes" else "no"

:w
fun foo(x) = if x is @do_not_compile Box' then "yes" else "no"
//│ ╔══[WARNING] This annotation is not supported here.
//│ ║  l.128: 	fun foo(x) = if x is @do_not_compile Box' then "yes" else "no"
//│ ║         	                     ^^^^^^^^^^^^^^^
//│ ╟── Note: Patterns only support the `@compile` annotation.
//│ ║  l.128: 	fun foo(x) = if x is @do_not_compile Box' then "yes" else "no"
//│ ╙──       	                                     ^^^^

