:js
:effectHandlers
:lift

let id = 0
//│ id = 0

// FIXME: if the following 2 blocks are together, lifter causes class ordering issue
class MaybeStop with
  fun f(x: Bool): ()

fun handleEffects(g) =
  handle h1 = MaybeStop with
    fun f(x)(k) =
      if x then
        print("h1 stop")
      else
        set id += 1
        let cur = id
        print("h1 start " + String(cur))
        let result = k(x)
        print("h1 end " + String(cur))
        result
  handle h2 = MaybeStop with
    fun f(x)(k) =
      if x then
        print("h2 stop")
      else
        set id += 1
        let cur = id
        print("h2 start " + String(cur))
        let result = k(x)
        print("h2 end " + String(cur))
        result
  g(h1, h2)
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:Handler$h1$f$ is not CPS-transformed because it has more than one parameter list.
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:Handler$h2$f$ is not CPS-transformed because it has more than one parameter list.
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:handleBlock$$ is not CPS-transformed because it has more than one parameter list.
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:handleBlock$$ is not CPS-transformed because it has more than one parameter list.


fun f(h1, h2) =
  h1.f(false)
  h2.f(false)
  h1.f(false)
  h2.f(false)
  h1.f(false)
  h2.f(false)
handleEffects(f)
//│ = "function cpsCont$5(retVal) {       runtime.checkArgs(\"cpsCont$5\", 1, true, arguments.length);       let cpsCont$4;       tmp1 = retVal;       tmp2 = \"h1 start \" + tmp1;       runtime.checkCall(Predef.print(tmp2));       cpsCont$4 = function cpsCont$4(retVal1) {         runtime.checkArgs(\"cpsCont$4\", 1, true, arguments.length);         let cpsCont$3;         result = retVal1;         cpsCont$3 = function cpsCont$3(retVal2) {           runtime.checkArgs(\"cpsCont$3\", 1, true, arguments.length);           tmp3 = retVal2;           tmp4 = \"h1 end \" + tmp3;           runtime.checkCall(Predef.print(tmp4));           return runtime.checkCall(k(result))         };         return runtime.checkCall(globalThis.String(cpsCont$3, cur))       };       return runtime.checkCall(k1(cpsCont$4, x))     }"


fun f(h1, h2) =
  h1.f(false)
  h2.f(false)
  h1.f(false)
  h2.f(true)
  h1.f(false)
  h2.f(false)
handleEffects(f)
//│ = "function cpsCont$5(retVal) {       runtime.checkArgs(\"cpsCont$5\", 1, true, arguments.length);       let cpsCont$4;       tmp1 = retVal;       tmp2 = \"h1 start \" + tmp1;       runtime.checkCall(Predef.print(tmp2));       cpsCont$4 = function cpsCont$4(retVal1) {         runtime.checkArgs(\"cpsCont$4\", 1, true, arguments.length);         let cpsCont$3;         result = retVal1;         cpsCont$3 = function cpsCont$3(retVal2) {           runtime.checkArgs(\"cpsCont$3\", 1, true, arguments.length);           tmp3 = retVal2;           tmp4 = \"h1 end \" + tmp3;           runtime.checkCall(Predef.print(tmp4));           return runtime.checkCall(k(result))         };         return runtime.checkCall(globalThis.String(cpsCont$3, cur))       };       return runtime.checkCall(k1(cpsCont$4, x))     }"


fun f(h1, h2) =
  h1.f(false)
  h2.f(false)
  h1.f(true)
  h2.f(false)
  h1.f(false)
  h2.f(false)
handleEffects(f)
//│ = "function cpsCont$5(retVal) {       runtime.checkArgs(\"cpsCont$5\", 1, true, arguments.length);       let cpsCont$4;       tmp1 = retVal;       tmp2 = \"h1 start \" + tmp1;       runtime.checkCall(Predef.print(tmp2));       cpsCont$4 = function cpsCont$4(retVal1) {         runtime.checkArgs(\"cpsCont$4\", 1, true, arguments.length);         let cpsCont$3;         result = retVal1;         cpsCont$3 = function cpsCont$3(retVal2) {           runtime.checkArgs(\"cpsCont$3\", 1, true, arguments.length);           tmp3 = retVal2;           tmp4 = \"h1 end \" + tmp3;           runtime.checkCall(Predef.print(tmp4));           return runtime.checkCall(k(result))         };         return runtime.checkCall(globalThis.String(cpsCont$3, cur))       };       return runtime.checkCall(k1(cpsCont$4, x))     }"


class Eff with
  fun perform(): ()
data class Box(n)
let box = new mut Box(0)
//│ box = Box(0)


:expect 5120
fun f(h, box, n) =
  if n <= 1 then
    h.perform()
  else
    handle h2 = Eff with
      fun perform()(k) =
        set box.n = box.n + 1
        h.perform()
        k()
    f(h2, box, n - 1)
    f(h2, box, n - 1)
handle h = Eff with
  fun perform()(k) =
    set box.n = box.n + 1
    k()
f(h, box, 10)
box.n
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:Handler$h2$perform$ is not CPS-transformed because it has more than one parameter list.
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:handleBlock$$ is not CPS-transformed because it has more than one parameter list.
//│ FAILURE: Unexpected runtime error
//│ FAILURE LOCATION: processIRBlock (JSBackendDiffMaker.scala:316)
//│ ═══[RUNTIME ERROR] Expected: '5120', got: '10'
//│ = 10

handle h1 = Eff with
  fun perform()(k) =
    print("h1")
    let x = k()
    print("h1 end")
    x
handle h2 = Eff with
  fun perform()(k) =
    print("h2")
    h1.perform()
    let x = k()
    print("h2 end")
    x
h2.perform()
h2.perform()
h2.perform()
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:handleBlock$$ is not CPS-transformed because it has more than one parameter list.
//│ > h2
//│ > h1
//│ > h2
//│ > h1
//│ > h2
//│ > h1
//│ > h2 end
//│ > h2 end
//│ > h2 end
//│ > h1 end
//│ > h1 end
//│ > h1 end


handle h1 = Eff with
  fun perform()(k) =
    print("h1")
    k()
handle h2 = Eff with
  fun perform()(k) =
    print("h2")
    h1.perform()
    k()
handle h3 = Eff with
  fun perform()(k) =
    print("h3")
    h2.perform()
    k()
h1.perform()
h3.perform()
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:Handler$h3$perform$ is not CPS-transformed because it has more than one parameter list.
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:handleBlock$$ is not CPS-transformed because it has more than one parameter list.
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:handleBlock$$ is not CPS-transformed because it has more than one parameter list.
//│ > h1
//│ > h3
//│ > h2
//│ > h1
//│ = fun cpsId


let d = 0
//│ d = 0


:expect 11
handle h1 = Eff with
  fun perform()(k) =
    set d = d + 1
    k()
let i = 0
h1.perform()
while i < 10 do 
  handle h2 = Eff with
    fun perform()(k) =
      set d = d + 1
      k()
  h2.perform()
  set i = i + 1
d
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:handleBlock$$ is not CPS-transformed because it has more than one parameter list.
//│ = 11

abstract class Eff2 with
  fun f1(): ()
  fun f2(): ()


// Effect performed at end of handler, which trigger a special branch to handle
handle h1 = Eff2 with
  fun f1()(k) =
    print("h1.f1 pre")
    k()
    print("h1.f1 post")
  fun f2()(k) = k()
handle h2 = Eff2 with
  fun f1()(k) =
    print("h2.f1")
    k()
  fun f2()(k) =
    print("h2.f2 pre")
    k()
    print("h2.f2 post")
    h1.f1()
h2.f2()
h2.f1()
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:handleBlock$$ is not CPS-transformed because it has more than one parameter list.
//│ > h2.f2 pre
//│ > h2.f1
//│ > h2.f2 post
//│ > h1.f1 pre
//│ > h1.f1 post
