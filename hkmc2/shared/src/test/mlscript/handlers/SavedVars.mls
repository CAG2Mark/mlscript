:js
:effectHandlers
:lift


// This acts as a function that may throw an effect
fun f() = ()

// A constant function to block any potential optimization.
fun always_true() = true


// Observe that no local variables will be saved, as they are not needed.
:sjs
fun g() =
  let a =
    if always_true() then
      f()
      1
    else
      2
  a
//│ ————————————| JS (unsanitized) |————————————————————————————————————————————————————————————————————
//│ let g, Capture$scope01;
//│ (class Capture$scope0 {
//│   static {
//│     Capture$scope01 = this
//│   }
//│   constructor(scrut$0, tmp$1) {
//│     this.tmp$1 = tmp$1;
//│     this.scrut$0 = scrut$0;
//│   }
//│   toString() { return runtime.render(this); }
//│   static [definitionMetadata] = ["class", "Capture$scope0"];
//│ });
//│ g = function g(k) {
//│   let scope0$cap;
//│   scope0$cap = new Capture$scope01(undefined, undefined);
//│   scope0$cap.scrut$0 = true;
//│   if (scope0$cap.scrut$0 === true) { scope0$cap.tmp$1 = 1; return }
//│   scope0$cap.tmp$1 = 2;
//│   return;
//│ };
//│ —————————————————| Output |—————————————————————————————————————————————————————————————————————————
