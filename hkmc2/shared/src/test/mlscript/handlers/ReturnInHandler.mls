:js
:effectHandlers
:lift


abstract class Effect with
  fun f(): ()

// `return` within handle blocks is implemented as a non-local return
fun f() =
  handle h = Effect with
    fun foo()(r) =
      let m = r(())
      print("Bye!") // This never executes because `r` returns from `f`
      m
  h.foo()
  return 3 // Note: changing this to just `3` leads to `print("Bye!")` executing
f()
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:‹non-local return effect›ret$ is not CPS-transformed because it has more than one parameter list.
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:handleBlock$$ is not CPS-transformed because it has more than one parameter list.
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:handleBlock$$ is not CPS-transformed because it has more than one parameter list.
//│ = 3

fun f() =
  handle h = Effect with
    fun foo()(r) =
      let m = r()
      print("Bye!")
      m
  in
    h.foo()
    return 3
  10
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:‹non-local return effect›ret$ is not CPS-transformed because it has more than one parameter list.
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:handleBlock$$ is not CPS-transformed because it has more than one parameter list.
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:handleBlock$$ is not CPS-transformed because it has more than one parameter list.

:expect 3
f()
//│ = 3

:e
let l = () =>
  handle h = Effect with
    fun foo()(r) = r(())
  return 3
  4
//│ ╔══[COMPILATION ERROR] Return statements are not allowed outside of functions.
//│ ║  l.58: 	  return 3
//│ ╙──      	  ^^^^^^^^
//│ l = fun

:re
l()
//│ ═══[RUNTIME ERROR] This code cannot be run as its compilation yielded an error.

:e
handle h1 = Effect with
  fun foo()(r) = r(())
let l = () =>
  handle h2 = Effect with
    fun bar()(r) = r(())
  h1.foo()
  return 3
  4
l()
//│ ╔══[COMPILATION ERROR] Return statements are not allowed outside of functions.
//│ ║  l.76: 	  return 3
//│ ╙──      	  ^^^^^^^^
//│ ═══[RUNTIME ERROR] This code cannot be run as its compilation yielded an error.


