:js
:effectHandlers
:lift


abstract class Effect[A] with
  fun perform(arg: A): A


let
  xs = mut []
  count = 0
fun main() =
  handle g = Effect with
    fun perform(arg1)(k1) =
      print("B " + arg1)
      xs.push(k1)
  print(g.perform("a"))
  print("—")
  print(g.perform("b"))
  print("—")
  print(g.perform("c"))
main()
while
  do print(xs)
  let f = xs.pop()
  f !== () then
    set count += 1
    f(count)
    print("?")
//│ > B a
//│ > [fun]
//│ > 1
//│ > —
//│ > B b
//│ > ?
//│ > [fun]
//│ > 2
//│ > —
//│ > B c
//│ > ?
//│ > [fun]
//│ > 3
//│ > ?
//│ > []
//│ count = 3
//│ xs = []

// * Notes:
(mut []).pop() === undefined
//│ = false
(mut []).pop() === null
//│ = false
(mut []).pop() === ()
//│ = true


// g is non-resumptive, which means g.perform("a") does not resume
// hence it should exit the entire handle block with the return value of xs.push(k1)
:expect 1
handle h = Effect with
  fun perform(arg)(k) =
    print("A " + arg)
    let
      xs = mut []
      count = 0
    handle g = Effect with
      fun perform(arg1)(k1) =
        print("B " + arg + " " + arg1)
        xs.push(k1)
    k(g)
    while xs.pop() is
      null then print("Done.")
      f then
        set count += 1
        f(count)
let g = h.perform("hi")
print(g.perform("a"))
print("—")
print(g.perform("b"))
print("—")
print(g.perform("c"))
// set g = h.perform("hola")
// g.perform("adios")
//│ > A hi
//│ > B hi a
//│ > 1
//│ > —
//│ > B hi b
//│ > 2
//│ > —
//│ > B hi c
//│ > 3
//│ > unhandled error
//│ > unhandled error
//│ FAILURE: Unexpected runtime error
//│ FAILURE LOCATION: mkQuery (JSBackendDiffMaker.scala:259)
//│ ═══[RUNTIME ERROR] TypeError: scrut is not a function
//│     at handleBlock$$worker (REPL27:1:4847)
//│     at REPL27:1:6932
//│     at Runtime.enterHandleBlock (file:///storage/mark/Repos/mlscript/hkmc2/shared/src/test/mlscript-compile/Runtime.mjs:1233:28)
//│     at Handler$h$perform$worker (REPL27:1:3429)
//│     at EffectSig.handlerFun (REPL27:1:7972)
//│     at file:///storage/mark/Repos/mlscript/hkmc2/shared/src/test/mlscript-compile/Runtime.mjs:57:52
//│     at Object.try_catch (file:///storage/mark/Repos/mlscript/hkmc2/shared/src/test/mlscript-compile/RuntimeJS.mjs:15:18)
//│     at Runtime.handleEffect (file:///storage/mark/Repos/mlscript/hkmc2/shared/src/test/mlscript-compile/Runtime.mjs:1319:41)
//│     at Runtime.handleEffects (file:///storage/mark/Repos/mlscript/hkmc2/shared/src/test/mlscript-compile/Runtime.mjs:1254:23)
//│     at lambda3 (file:///storage/mark/Repos/mlscript/hkmc2/shared/src/test/mlscript-compile/Runtime.mjs:205:19)
//│ FAILURE: Unexpected runtime error
//│ FAILURE LOCATION: processIRBlock (JSBackendDiffMaker.scala:299)
//│ ═══[RUNTIME ERROR] Expected: '1', got: 'undefined'


