:js
:effectHandlers
:lift


abstract class ThreadEffect with
  val tasks = mut []
  fun start(thread: () -> ()): ()
  fun fork(thread: () -> ()): ()
  fun yld(): ()

class Lock(val locked) with
  fun lock(th: ThreadEffect) =
    while locked do
      th.yld()
    set locked = true
  fun unlock() =
    set locked = false
//│ FAILURE: Unexpected internal error
//│ FAILURE LOCATION: thisError (Scope.scala:55)
//│ FAILURE INFO: Tuple2:
//│   _1 = Scope:
//│     parentOrCfg = Left of Cfg:
//│       escapeChars = true
//│       useSuperscripts = false
//│       includeZero = false
//│       defaultName = "tmp"
//│     curThis = S of S of globalThis:globalThis⁰
//│     bindings = HashMap(class:Lock -> Lock, member:lock$worker -> lock$worker, tmp:runtime -> runtime, tmp:block$res -> block$res, tmp:definitionMetadata -> definitionMetadata, tmp:prettyPrint -> prettyPrint, tmp:block$res -> block$res2, tmp:Term -> Term, tmp:Block -> Block, tmp:option -> option, member:ThreadEffect -> ThreadEffect1, class:ThreadEffect -> ThreadEffect, member:Predef -> Predef, tmp:block$res -> block$res1, member:Lock -> Lock1)
//│   _2 = class:Lock⁰
//│ ═══[INTERNAL ERROR] `this` not in scope: class:Lock
//│ FAILURE: Unexpected internal error
//│ FAILURE LOCATION: thisError (Scope.scala:55)
//│ FAILURE INFO: Tuple2:
//│   _1 = Scope:
//│     parentOrCfg = Left of Cfg:
//│       escapeChars = true
//│       useSuperscripts = false
//│       includeZero = false
//│       defaultName = "tmp"
//│     curThis = S of S of globalThis:globalThis⁰
//│     bindings = HashMap(class:Lock -> Lock, member:lock$worker -> lock$worker, tmp:runtime -> runtime, tmp:block$res -> block$res, tmp:definitionMetadata -> definitionMetadata, tmp:prettyPrint -> prettyPrint, tmp:block$res -> block$res2, tmp:Term -> Term, tmp:Block -> Block, tmp:option -> option, member:ThreadEffect -> ThreadEffect1, class:ThreadEffect -> ThreadEffect, member:Predef -> Predef, tmp:block$res -> block$res1, member:Lock -> Lock1)
//│   _2 = class:Lock⁰
//│ ═══[INTERNAL ERROR] `this` not in scope: class:Lock
//│ FAILURE: Unexpected internal error
//│ FAILURE LOCATION: thisError (Scope.scala:55)
//│ FAILURE INFO: Tuple2:
//│   _1 = Scope:
//│     parentOrCfg = Left of Cfg:
//│       escapeChars = true
//│       useSuperscripts = false
//│       includeZero = false
//│       defaultName = "tmp"
//│     curThis = S of S of globalThis:globalThis⁰
//│     bindings = HashMap(class:Lock -> Lock, member:lock$worker -> lock$worker, tmp:runtime -> runtime, tmp:block$res -> block$res, tmp:definitionMetadata -> definitionMetadata, tmp:prettyPrint -> prettyPrint, tmp:block$res -> block$res2, tmp:Term -> Term, tmp:Block -> Block, tmp:option -> option, member:ThreadEffect -> ThreadEffect1, class:ThreadEffect -> ThreadEffect, member:Predef -> Predef, tmp:block$res -> block$res1, member:Lock -> Lock1)
//│   _2 = class:Lock⁰
//│ ═══[INTERNAL ERROR] `this` not in scope: class:Lock
//│ > let Lock1, lock$worker;try { lock$worker = function lock$worker(lock$varsClass) {   runtime.checkArgs("lock$worker", 1, true, arguments.length);   let scrut, pc;   pc = lock$varsClass.at(0);   main: while (true) {     switch (pc) {       case 3:         ‹MISSING_THIS›.locked = true;         runtime.checkCall(runtime.popFrame());         return runtime.Unit;       case 2:         runtime.resumeValue;         lock$varsClass[0] = 1;         pc = 1;         continue main;       case 0:         lock$varsClass[0] = 1;         pc = 1;         scrut = ‹MISSING_THIS›.locked;         if (scrut === true) {           lock$varsClass[0] = 2;           runtime.resumeValue = runtime.checkCall(lock$varsClass.at(1).yld());           pc = 2;           continue main         }         lock$varsClass[0] = 3;         pc = 3;         continue main;       case 1:         scrut = ‹MISSING_THIS›.locked;         if (scrut === true) {           lock$varsClass[0] = 2;           runtime.resumeValue = runtime.checkCall(lock$varsClass.at(1).yld());           pc = 2;           continue main         }         lock$varsClass[0] = 3;         pc = 3;         continue main;     }     break;   } }; runtime.checkCall(runtime.resetEffects()); Lock1 = function Lock(locked) {   return globalThis.Object.freeze(new Lock.class(locked)); }; globalThis.Object.freeze(class Lock {   static {     Lock1.class = this   }   constructor(locked) {     this.locked = locked;   }   get lock$__checkNotMethod() { runtime.deboundMethod("lock", "Lock"); }    get unlock$__checkNotMethod() { runtime.deboundMethod("unlock", "Lock"); }   lock(th) {     runtime.checkArgs("lock", 1, true, arguments.length);     let tmp;     tmp = [       0,       th     ];     runtime.checkCall(runtime.pushFrame(lock$worker, tmp));     return runtime.checkCall(lock$worker(tmp))   }   unlock() {     runtime.checkArgs("unlock", 0, true, arguments.length);     this.locked = false;     return runtime.Unit   }   toString() { return runtime.render(this); }   static [definitionMetadata] = ["class", "Lock", ["locked"]]; });; undefined } catch (e) { console.log('\u200B' + (e.stack ?? e) + '\u200B'); }
//│ FAILURE: Unexpected compilation error
//│ FAILURE LOCATION: mkQuery (JSBackendDiffMaker.scala:251)
//│ ═══[COMPILATION ERROR] [Uncaught SyntaxError] Invalid or unexpected token

fun f(h, x) =
  print("f " + x)
  h.yld()
  print("f " + x)
  h.yld()
  print("f " + x)

fun main(h) =
  print("main start")
  h.fork(() => f(h, 0))
  h.fork(() => f(h, 1))
  h.fork(() => f(h, 2))
  print("main end")


// LIFO
handle h = ThreadEffect with
  fun fork(thread)(k) =
    this.tasks.unshift(thread)
    k(())
  fun yld()(k) =
    this.tasks.push(() => k(()))
    enterHandleBlock(this, () => this.tasks.shift()())
  fun start(main)(k) =
    this.tasks.push(() => main(this))
    while this.tasks.length != 0 do
      enterHandleBlock(this, () => this.tasks.shift()())
    k(())
in
  h.start(main)
//│ > main start
//│ > main end
//│ > f 2
//│ > f 1
//│ > f 0
//│ > f 2
//│ > f 1
//│ > f 0
//│ > f 2
//│ > f 1
//│ > f 0


// FIFO
handle h = ThreadEffect with
  fun fork(thread)(k) =
    this.tasks.push(thread)
    k(())
  fun yld()(k) =
    this.tasks.push(() => k(()))
    enterHandleBlock(this, () => this.tasks.shift()())
  fun start(main)(k) =
    this.tasks.push(() => main(this))
    while this.tasks.length != 0 do
      enterHandleBlock(this, () => this.tasks.shift()())
    k(())
in
  h.start(main)
//│ > main start
//│ > main end
//│ > f 0
//│ > f 1
//│ > f 2
//│ > f 0
//│ > f 1
//│ > f 2
//│ > f 0
//│ > f 1
//│ > f 2


// Adapted from: https://ocaml.org/manual/5.3/effects.html

abstract class ThreadEffect with
  fun fork(thread: ThreadEffect -> ()): ()
  fun yld(): ()

val tasks = mut []
//│ tasks = []

fun f(x)(h) =
  print("f " + x)
  h.yld()
  print("f " + x)
  h.yld()
  print("f " + x)

fun main(h) =
  print("main start")
  h.fork(f(0))
  h.fork(f(1))
  h.fork(f(2))
  print("main end")

fun dequeue() =
  if tasks.length != 0 do
    tasks.shift()(())

fun start(thread: ThreadEffect -> ()) =
  handle h = ThreadEffect with
    fun fork(new_thread)(k) =
      tasks.push(k)
      start(new_thread)
    fun yld()(k) =
      tasks.push(k)
      dequeue()
  in
    thread(h)
  dequeue()

start(main)
//│ > main start
//│ > f 0
//│ > f 1
//│ > f 0
//│ > f 2
//│ > f 1
//│ > f 0
//│ > main end
//│ > f 2
//│ > f 1
//│ > f 2


import "../../mlscript-compile/Option.mls"
open Option

abstract class ThreadEffect with
  fun fork(thread: ThreadEffect -> ()): ()
  fun yld(): ()
  fun xchg(x: Int): Int

val tasks = mut []
fun enqueue(k, x) =
  tasks.push(() => k(x))
fun dequeue() =
  if tasks.length != 0 do
    tasks.shift()()
//│ tasks = []

let exchanger = None
//│ exchanger = None

fun start(thread: ThreadEffect -> ()) =
  handle h = ThreadEffect with
    fun fork(new_thread)(k) =
      enqueue(k, ())
      start(new_thread)
    fun yld()(k) =
      enqueue(k, ())
      dequeue()
    fun xchg(x)(k) =
      if exchanger is
        Some([y, k1]) then
          set exchanger = None
          enqueue(k1, x)
          k(y)
        None then
          set exchanger = Some([x, k])
          dequeue()
  in
    thread(h)
  dequeue()

fun main(h) =
  h.fork(h =>
    print("[t1] Sending 0")
    let v = h.xchg(0)
    print("[t1] received " + v.toString()))
  h.fork(h =>
    print("[t2] Sending 1")
    let v = h.xchg(1)
    print("[t2] received " + v.toString()))

start(main)
//│ > [t1] Sending 0
//│ > [t2] Sending 1
//│ > [t2] received 0
//│ > [t1] received 1
