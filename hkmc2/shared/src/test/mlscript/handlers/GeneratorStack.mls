:js
:effectHandlers
:lift

import "../../mlscript-compile/Stack.mls"
open Stack

class Generator with
  fun produce(result: Stack[Int]): ()

fun (++) concat(l1, l2) =
  if l1 is 
    Nil then l2
    Cons(h, t) then Cons(h, t ++ l2)

fun (:+) append(l, e) =
  if l is 
    Nil then e :: Nil
    Cons(h, t) then h :: t :+ e

fun length(l) =
  if l is
    Nil then 0
    Cons(_, t) then 1 + length(t)

fun permutations_impl(gen, l1, l2) =
  if l2 is
    Nil and
      l1 is Nil then gen.produce(Nil)
      else ()
    Cons(f, t) then
      handle gen2 = Generator with
        fun produce(result)(resume) =
          gen.produce(f :: result)
          resume(())
      permutations_impl(gen2, Nil, l1 ++ t)
      permutations_impl(gen, l1 :+ f, t)
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:Handler$gen2$produce$ is not CPS-transformed because it has more than one parameter list.
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:handleBlock$$ is not CPS-transformed because it has more than one parameter list.

fun permutations(gen, l) =
  permutations_impl(gen, Nil, l)

let result =
  handle gen = Generator with
    fun produce(result)(resume) =
      print(result)
      let r = resume(())
      result :: r
  permutations(gen, 1 :: 2 :: 3 :: 4 :: Nil), Nil
in length(result)
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:Handler$gen$produce$ is not CPS-transformed because it has more than one parameter list.
//│ > Cons(1, Cons(2, Cons(3, Cons(4, Nil))))
//│ FAILURE: Unexpected runtime error
//│ FAILURE LOCATION: mkQuery (JSBackendDiffMaker.scala:276)
//│ ═══[RUNTIME ERROR] Error: match error
//│     at length (REPL22:1:694)
//│     at cpsCont$4 (REPL31:1:2253)
//│     at Runtime.handlerTrampoline (file:///storage/mark/Repos/mlscript/hkmc2/shared/src/test/mlscript-compile/Runtime.mjs:911:29)
//│     at Runtime.cpsHandlerImpl (file:///storage/mark/Repos/mlscript/hkmc2/shared/src/test/mlscript-compile/Runtime.mjs:916:20)
//│     at REPL31:1:2326
//│     at ContextifyScript.runInThisContext (node:vm:137:12)
//│     at defaultEval (node:repl:610:24)
//│     at REPLEvalInContext (node:repl:693:9)
//│     at AsyncLocalStorage.run (node:internal/async_local_storage/async_context_frame:65:14)
//│     at REPLServer.REPLEval [as eval] (node:repl:692:19)
