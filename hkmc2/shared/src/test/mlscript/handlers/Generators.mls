:js
:effectHandlers
:lift

abstract class Generator with
  fun produce(result): ()

fun permutations_impl(gen, l1, l2) =
  if l2 is
    [f, ...t] do
      handle genWithPrefix = Generator with
        fun produce(result)(resume) =
          result.unshift(f)
          gen.produce(result)
          resume(())
      permutations_impl(genWithPrefix, mut [], l1.concat(t))
      l1.push(f)
      permutations_impl(gen, l1, t)
    [] and l1 is [] do
      gen.produce(mut [])
fun permutations(gen, l) =
  permutations_impl(gen, mut [], l)
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:Handler$genWithPrefix$produce$ is not CPS-transformed because it has more than one parameter list.
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:handleBlock$$ is not CPS-transformed because it has more than one parameter list.

let res = mut []
handle gen = Generator with
  fun produce(result)(resume) =
    res.push(result)
    resume(())
in permutations(gen, [1, 2, 3, 4])
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:Handler$gen$produce$ is not CPS-transformed because it has more than one parameter list.
//│ res = []

fun permutations_foreach(l, f) =
  handle gen = Generator with
    fun produce(result)(resume) =
      f(result)
      resume(())
  permutations(gen, l)
permutations_foreach([1, 2, 3], print)
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:Handler$gen$produce$ is not CPS-transformed because it has more than one parameter list.
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:handleBlock$$ is not CPS-transformed because it has more than one parameter list.


fun permutations_impl(gen, l1, l2) =
  if l2 is
    [f, ...t] do
      handle genWithPrefix = Generator with
        fun produce(result)(resume) =
          gen.produce([f, ...result])
          let x = resume(())
          x
      permutations_impl(genWithPrefix, mut [], l1.concat(t))
      l1.push(f)
      permutations_impl(gen, l1, t)
    [] and l1 is [] do
      gen.produce([])
fun permutations(gen, l) =
  permutations_impl(gen, mut [], l)
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:Handler$genWithPrefix$produce$ is not CPS-transformed because it has more than one parameter list.
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:handleBlock$$ is not CPS-transformed because it has more than one parameter list.

let res = mut []
handle gen = Generator with
  fun produce(result)(resume) =
    res.push(result)
    let x = resume(())
    x
in permutations(gen, [1, 2, 3])
//│ FAILURE: Unexpected warning
//│ FAILURE LOCATION: applyFunDefn (CpsHandlerLowering.scala:205)
//│ ═══[WARNING] The function term:Handler$gen$produce$ is not CPS-transformed because it has more than one parameter list.
//│ res = []
